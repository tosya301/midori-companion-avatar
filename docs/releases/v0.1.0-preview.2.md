泳装动画素材修正版；保留上一预览版，使用新版本号区分下载包内容。

## 本次更新

- 更新泳装奶绿动态 SVG，采用维护者提供的摇晃重制版素材。
- 保留现有立绘顺序、普通／近距模式的大小与位置、夜间透明显示和其他功能；未改动宿主动画控制代码。
- 本次不包含本地音乐／歌词试听功能。

## 下载与升级

下载 `midori-companion-avatar-v0.1.0-preview.2.zip`，解压到新目录，使用 Python 3.10+ 运行 `python server.py`，打开 `http://127.0.0.1:5178/`。也可使用原有启动脚本。

上一版本用户也可只替换 `assets/sway/swim-milky-green-animated.svg` 后刷新网页；先备份旧文件，不要覆盖自己的本地配置。已运行实例如仍显示旧素材，请刷新页面。

随附 `.sha256` 用于校验 ZIP；包内 `RELEASE_MANIFEST.json` 记录该版本源码提交与全部文件哈希。

## 本次验证

- 公开运行时 Python 44 项、JavaScript 4 项测试通过。
- 私人版与公开版的 SVG 原生文档、主题透明度浏览器测试均通过；源 ZIP、两份运行文件及 HTTP 返回字节一致。
- 摇晃、眨眼、Freeze、近距切换、失败恢复与窄屏兼容检查通过。保留其他立绘与宿主代码，不代表 Windows 硬件持续帧率已全面验收。

## 范围与限制

这是素材修正版，仍为公开预览版，不将本次替换表述为全平台性能保证。原版接入与素材许可边界保持不变；其他歌词、Agent、TTS 及账号集成不在本次变更范围内。素材权利不随代码许可证一揽子改授，参见 `ASSET_RIGHTS.md` 与 `LICENSE_SCOPE.md`。
