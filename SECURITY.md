# 本地安全与隐私

## 默认边界

- 仅绑定 `127.0.0.1`。这是本机受信任工具，不是多租户网络服务；不要用端口转发、反向代理或公开隧道直接暴露。
- Agent 写请求需要本地 Bearer 令牌。令牌存在被 Git 忽略的 `.local/`，不要粘贴到网页、截图、README、日志、URL 或聊天。
- 浏览器写请求使用严格同源检查与专用请求头；不开放任意 CORS。Host 验证用于降低 DNS rebinding 风险。
- 仅有限静态路径可访问，禁止目录列出、目录穿越和通过软链接读取外部文件。源码配置与运行目录不能直接通过静态文件服务获取。
- TTS／聊天服务 URL 由受信任的本机操作者配置，不接受每条消息传入任意读取文件路径或后端抓取地址。供应商重定向不得携带凭据继续跟随。

## 不随包提供的权限

候选版不附私人 profile、账号 token、OAuth 客户端、Cookie、邮件记录、动态标识、历史音频或聊天记录。不提供邮箱删除、社交发帖、账号登录和系统任意命令执行。漂浮图标中的 Context 是意图路由，不是权限凭证。

## 使用期间的数据

- 待朗读文字会发送到你明确选择的 TTS 服务；聊天内容会发送到你配置的 Agent 适配服务。各服务有自己的留存与计费政策。
- 本地产生的令牌／音频不能因为有 `.gitignore` 就被当成可以公开：普通文件夹压缩可能仍会包含它们。
- 浏览器本地存储中有界面偏好、会话标识和最近音频文本／地址；“清空最近事件”只清理该列表，不代表删除供应商或 Agent 自己的记录。
- 不要朗读内部推理、工具输出、终端日志、密钥或未获用户同意的敏感内容。只交付适合用户看到的最终回复。
- 请求超时或断开不代表操作没有执行；不要自动重复不确定的写请求。按 API 文档使用同一请求 ID 查询／去重语义。

## 安全报告与发布前检查

请不要在公开 Issue 中粘贴凭据或个人材料。维护者公开报告渠道尚待正式发布时设置。发布前核对最终 Git 树、压缩包、素材权利与 Folia 源码下载，不仅检查工作目录或 `.gitignore`。

当前只制作本地候选版，没有上传 GitHub。测试范围及未验证项见 `docs/VERIFICATION.md`。
